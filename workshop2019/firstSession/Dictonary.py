
# dictonary are like associative array, works as key value pair, no two key should be same.

value = 10
d = {'key': value, "name": 'shintu', 'stream': "ece"}

# d.clear(       d.fromkeys(    d.items(       d.pop(         d.setdefault(  d.values(
# d.copy(        d.get(         d.keys(        d.popitem(     d.update(


print(d)

print(d.values())
